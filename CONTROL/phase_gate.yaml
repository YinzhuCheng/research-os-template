current_phase: research-os-v3.3
intervention_level: low
stage_gate_required: true
external_write_requires_confirmation: true
public_export_requires_confirmation: true
submission_requires_confirmation: true
budget_overrun_requires_confirmation: true
next_required_decision: "Review the material-first Copilot bridge, install the repo-scoped plugin if desired, and decide whether to use a heartbeat automation for pending intake detection."
confirmed_decisions:
  - "Use a material-first intake: initial free text or upload, followed by three targeted questions."
  - "Use plugin + MCP + browser cockpit as the preferred bridge shape."
  - "Browser submissions enqueue work; Codex asks for confirmation before execution."
  - "Raw uploaded material defaults to PRIVATE/intake at runtime and is not committed."
  - "Reference downloads are convenience scripts for lawful open sources or landing pages, not paywall bypass."
