# Failed Experiment Replay Fixture

This fixture confirms replay/eval logic can represent failed, partial, blocked,
cancelled, timed out, and unverified terminal states.

The template does not run a real experiment.
