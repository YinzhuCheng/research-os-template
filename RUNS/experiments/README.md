# Experiment Runs

This directory stores sanitized experiment run records and replay metadata.

Raw logs, private data, credentials, and restricted artifacts must not be placed
here unless a work order explicitly allows the path and privacy class. Use
`PRIVATE/` for runtime raw intake material and preserve audit metadata in
`PROVENANCE/`.
