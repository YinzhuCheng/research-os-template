# Export Policy

Allowed export sources:

- `PUBLIC/`
- `PROVENANCE/` after privacy scan

Forbidden export sources:

- `PRIVATE/`
- `.git/`
- `exports/`
- raw model payloads,
- credentials or authorization material.

If a researcher wants to publish private-derived information, create a sanitized public summary first.
